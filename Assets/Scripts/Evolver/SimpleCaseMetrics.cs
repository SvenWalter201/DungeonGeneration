using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class SimpleCaseMetrics
{
    public const int maxWidth = 20, maxHeight = 20, popSize = 25, startNodes = 200, maxGenerations = 2000, logInterval = 2;
    public const float tileMutationChance = 0.02f;
}
